- name: GETTING CONFIGURATION BACKUP
  hosts: FORTIGATE_DC
  gather_facts: false
  connection: local

  vars:
    backup_dir: "/home/dxc-network/Bureau"

  tasks:
    - name: Check API token is injected
      ansible.builtin.debug:
        msg: "API token length: {{ api_key | length }}"
